-- {{description}}
--
-- Migration {{next_migration_number}} — added {{today_date}}.
--
-- TODO: describe the schema change. Remember to update `CLAUDE.md` and
-- `ARCHITECTURE.md` if this introduces a new user-visible concept.

-- CREATE TABLE IF NOT EXISTS {{name}} (
--     id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--     created_at TIMESTAMPTZ NOT NULL DEFAULT now()
-- );
